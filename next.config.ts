import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel gestisce nativamente Next.js — nessuna configurazione speciale necessaria.
  // NON impostare output: "export" perché rompe il runtime di Vercel.
};

export default nextConfig;
